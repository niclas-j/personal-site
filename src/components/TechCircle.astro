---
import { Image } from "astro:assets";

export interface Props {
  img?: ImageMetadata;
  name: string;
}

const { img, name } = Astro.props;
const imageSize = 20;
---

<div class="inline-flex px-2 py-1 items-center rounded-full border text-sm dark:bg-neutral-800 dark:text-white dark:border-neutral-700">
  {img && <Image src={img} width={imageSize} height={imageSize} alt={`${name} Logo`} />}
  <div class="ml-2">{name}</div>
</div>

<!-- <div class="flex space-y-3 overflow-hidden p-4 bg-white items-center justify-end text-center flex-col rounded-full shadow-sm h-32 w-32">
  <Image src={img} width={imageSize} height={imageSize} alt={`${name} Logo`} />
  <div>{name}</div>
</div> -->
